This is a very basic recreation of a Threads app where users can post messages to a global content pool for all other users to see.

Few features are enabled but basic posting and commenting is active.
